import 'package:flutter/material.dart';

class AccountTreeInitial extends StatelessWidget {
  const AccountTreeInitial({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
